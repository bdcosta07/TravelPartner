<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="?android:attr/selectableItemBackground"
    android:padding="@dimen/tile_padding">

    <ImageView
        android:id="@+id/gallery_image"
        android:layout_width="match_parent"
        android:layout_height="@dimen/tile_height"
        android:scaleType="center" />

    <TextView
        android:id="@+id/tvDateTaken"
        android:layout_width="match_parent"
        android:layout_height="@dimen/tile_textview_height"
        android:background="@color/medium_gray_with_transparency"
        android:layout_alignBottom="@+id/gallery_image"
        android:gravity="center_horizontal"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:padding="14dp"
        android:textSize="@dimen/place_type_titles" />
</RelativeLayout>